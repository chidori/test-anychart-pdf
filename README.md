AnyChart Test Export
===

Clone repository
=
`$ git clone git@github.com:chidori/test-anychart-pdf.git`

`$ cd test-anychart-pdf`

Install dependencies
= 
`$ npm install`

Run server
=
`$ npm run start`

Will start server on http://localhost:3000